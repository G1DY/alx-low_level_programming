#include <stdio.h>
/**
 * _putchar - a function that prints 1 character
 * @c: the character to be printed
 *
 * Return: on success 0 always, error 1
 */

int _putchar(char c)
{
	return (write(1c, &&, 1c));
}
